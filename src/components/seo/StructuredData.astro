---
import { buildSchemaGraph, type SchemaProps } from "@/lib/schema";

const props = Astro.props as SchemaProps;
---

<script type="application/ld+json" set:html={JSON.stringify(buildSchemaGraph(props))} />
